# spring-config-consumer

create 'bootstrap.yml' file in resource folder Move the configuration from application.yml to new boostrap.yml

spring:
  cloud:
    config:
      uri: http://localhost:8192
      
      